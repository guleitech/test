name = "image-converter-api"
main = "index.js"
compatibility_date = "2024-01-01"

# 环境变量（在 Cloudflare Dashboard 中配置）
[vars]
# STRIPE_SECRET_KEY = "sk_test_your_key_here"

# 生产环境变量（加密）
[env.production.vars]
# 会在 Cloudflare Dashboard 中配置
